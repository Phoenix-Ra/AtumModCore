plugins {
    id 'java'
}

group = mod_group
version = rootProject.version
dependencies {
    compileOnly compileOnly(project(':atumod-api',))
    compileOnly compileOnly(project(':atumod-core',))
}

